"use server"


export const getGeneralFeedPosts = async (
  userId: string,
  pageParam: number
) => {
  const limit = 10;
  const skip = pageParam * limit;

  try {
    
    
  } catch (error) {
    
  }




};
